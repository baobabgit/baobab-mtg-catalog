# Checklist interne — readiness audit v1.0.0 (GO / NO GO)

Document de travail pour valider qu’une release **1.0.0** peut être publiée sans surprise. Cocher au fil des itérations ; une case non satisfaite peut justifier un **NO GO** ou une action correctrice avant tag.

## Périmètre produit

- [x] Le README décrit clairement périmètre inclus / exclu et le rôle dans l’écosystème Baobab.
- [x] La distinction `CardDefinition` / `CardPrinting` / `Set` est documentée et reflétée dans le code.
- [x] L’import idempotent et la consultation (façade + filtres) sont illustrés par des exemples exécutables ou testés.

## Qualité et conformité

- [x] `pytest` passe avec couverture **≥ 90 %** (seuil `pyproject.toml`).
- [x] `black`, `flake8`, `mypy` (`src`), `pylint` (`src` + `tests`), `bandit` (`src`) : aucune erreur bloquante.
- [x] Contraintes `docs/000_dev_constraints.md` respectées (structure, une classe par fichier, types, docstrings publiques, exceptions racine).

## API publique et stabilité

- [x] Les exports documentés du package racine (`__all__`) sont stables et intentionnels.
- [x] Pas de fuite obligatoire d’implémentations in-memory pour un usage production documenté (injection de repositories explicitée).
- [x] `CHANGELOG.md` à jour pour la version publiée ; version `pyproject.toml` alignée (**1.0.0**).

## Dépendances et sécurité

- [x] Plages de versions des dépendances runtime (`baobab-scryfall-api-caller`, Python) revues.
- [x] Aucune dépendance interdite par le périmètre (UI, HTTP métier, collection, moteur de règles) n’a été introduite.

## Documentation et support développeur

- [x] `docs/001_specifications.md` cohérent avec le comportement livré (périmètre, entités, import, consultation).
- [x] Journal `docs/dev_diary.md` mentionne les décisions d’architecture récentes.
- [x] Procédure d’installation et de tests reproduite sur environnement propre **(CI GitHub Actions sur `main` + PR)**.

## Release technique

- [x] Tag Git `v1.0.0` sur le commit de release ; build wheel/sdist OK (job **package** CI + `python -m build` local).
- [x] Publication registre : validation **dry-run** via `twine check dist/*` dans la CI (métadonnées et structure des artefacts sans upload PyPI).

---

## État de l’audit (synthèse)

- **Critères cochés** : **16 / 16**.
- **Interprétation** : critères de la checklist **satisfaits** pour un audit **GO** technique sur cette base. L’**upload** effectif vers PyPI (ou registre interne) reste une action de déploiement manuelle hors dépôt, une fois les secrets et la politique Baobab en place.

*Dernière mise à jour : release **1.0.0** — CI, `twine check`, tag `v1.0.0`.*
